/**
 * For each day, returns how many days pass until a strictly faster (strictly
 * smaller) build time appears, or 0 if no later build is ever faster.
 *
 * O(n) via a monotonic stack of unresolved day indices. Must not mutate the
 * input array.
 */
export function daysUntilFasterBuild(buildTimes: number[]): number[] {
  // TODO: implement
  throw new Error('Not implemented');
}
