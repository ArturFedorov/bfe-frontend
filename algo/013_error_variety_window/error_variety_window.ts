/**
 * Returns the length of the longest contiguous window of the error log
 * containing at most `k` distinct error codes.
 *
 * @param codes - error codes in time order
 * @param k - maximum number of distinct codes allowed in the window (>= 0)
 * @returns length of the longest qualifying window (0 when `k` is 0 or the log is empty)
 */
export function errorVarietyWindow(codes: string[], k: number): number {
  // TODO: implement
  throw new Error('Not implemented');
}
